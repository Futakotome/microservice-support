server:
  port: 9020
  servlet:
    context-path: /cache
spring:
  application:
    name: cache-service
  profiles:
    active: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
